{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash",
            "shell": "bash",
            "args": [
              "-c",
              "r=${CLAUDE_PLUGIN_ROOT:-}; r=$(printf %s \"$r\" | perl -pe 's@\\\\@/@g'); [ -n \"$r\" ] || r=$(find \"${HOME:-}\"/.claude/plugins -maxdepth 8 -type d -path '*session-sync*/scripts' 2>/dev/null | sort | tail -1 | sed 's#/scripts##'); exec bash \"$r/scripts/import.sh\""
            ]
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash",
            "shell": "bash",
            "args": [
              "-c",
              "r=${CLAUDE_PLUGIN_ROOT:-}; r=$(printf %s \"$r\" | perl -pe 's@\\\\@/@g'); [ -n \"$r\" ] || r=$(find \"${HOME:-}\"/.claude/plugins -maxdepth 8 -type d -path '*session-sync*/scripts' 2>/dev/null | sort | tail -1 | sed 's#/scripts##'); exec bash \"$r/scripts/export.sh\""
            ]
          }
        ]
      }
    ]
  }
}